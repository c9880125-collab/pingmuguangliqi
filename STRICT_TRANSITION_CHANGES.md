# 严格逐档切换改进

版本：1.2.0

## 原问题

旧实现每发出一个刷新率命令后固定等待 800ms，然后直接发送下一档命令。系统显示服务、SurfaceFlinger 与厂商面板驱动均为异步处理；设备忙碌、息屏唤醒或厂商策略介入时，上一档尚未真正生效，下一档命令已经到达，因此会出现“漏一档”或直接跨档。

此外，旧实现每次都调用私有 SurfaceFlinger transaction 1035，并假设 `sfIndex = modeId - 1`。该私有接口和索引在不同系统版本/厂商实现中并不稳定，可能选错模式。

## 新逻辑

1. 每次切换前重新扫描当前系统模式，避免使用过期 modeId。
2. 使用当前活动显示模式作为起点，不再优先使用瞬时面板 FPS。
3. 自动生成当前分辨率下的完整升档/降档路径。
4. 每一档先通过 `cmd display set-user-preferred-display-mode` 与系统设置请求。
5. 轮询活动模式；必须连续两次确认进入目标档位，才允许进入下一档。
6. 确认后继续保持约 650~900ms，避免刚进入又被下一条命令覆盖。
7. 单档失败最多重试 4 次；后续重试才启用 SurfaceFlinger 私有接口作为兜底。
8. 某一档始终无法确认时立即停止，不再继续跳到更高/更低档位。
9. 失败后按已确认路径反向回退到切换前模式。
10. 全局串行化切换；新的切换请求会取消旧请求，避免手动切换与逐应用服务互相抢占。
11. 移除手动页面外层“整条路径重复执行三次”的逻辑，避免重复命令再次覆盖中间档位。

## 日志关键字

运行日志中搜索：

- `STRICT start`：本次路线
- `STRICT request`：发送某一档请求
- `STRICT poll`：当前实际活动档位
- `STRICT confirmed`：该档已确认并稳定
- `STRICT timeout`：该档等待超时
- `STRICT rollback`：失败后回退
- `STRICT success`：完整路线成功

## 注意

应用只能严格执行系统已经枚举的显示模式。若驱动将某个模式列出但从不真正进入，该档会被判定失败并回退，不会伪装成成功。
